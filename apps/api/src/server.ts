import "dotenv/config";
import express from "express";
import cors from "cors";
import jwt from "jsonwebtoken";
import { PrismaClient, BookingStatus } from "@prisma/client";
import { z } from "zod";
const prisma=new PrismaClient(),app=express();app.use(cors());app.use(express.json());
const PORT=Number(process.env.PORT??4000),SECRET=process.env.JWT_SECRET??"dev-secret";
type U={id:string;name:string;phone:string;role?:string|null;teamName?:string|null};const sign=(u:U)=>jwt.sign(u,SECRET,{expiresIn:"30d"});
const select={id:true,name:true,phone:true,role:true,teamName:true,captainQrData:true,isPlayer:true,isCaptain:true} as const;const getUser=(id:string)=>prisma.user.findUnique({where:{id},select});
function auth(req:express.Request,res:express.Response,next:express.NextFunction){const h=req.headers.authorization;if(!h?.startsWith("Bearer "))return res.status(401).json({error:"Unauthorized"});try{(req as any).user=jwt.verify(h.slice(7),SECRET);next()}catch{return res.status(401).json({error:"Invalid token"})}}
const otp=new Map<string,{otp:string;expires:number}>();app.get("/health",(_,r)=>r.json({ok:true}));
app.post("/auth/send-otp",async(req,res)=>{const phone=z.string().min(8).parse(req.body.phone).trim();otp.set(phone,{otp:"123456",expires:Date.now()+300000});res.json({ok:true,devOtp:"123456"})});
app.post("/auth/verify-otp",async(req,res)=>{const b=z.object({phone:z.string().min(8),otp:z.string().length(6),name:z.string().min(2).optional()}).parse(req.body),phone=b.phone.trim(),s=otp.get(phone);if(!s||s.expires<Date.now()||s.otp!==b.otp)return res.status(400).json({error:"Invalid or expired OTP."});let u=await prisma.user.findUnique({where:{phone}});if(!u)u=await prisma.user.create({data:{phone,name:b.name??"New Player",isPlayer:true}});else if(!u.isPlayer&&!u.isCaptain)u=await prisma.user.update({where:{id:u.id},data:{isPlayer:true}});otp.delete(phone);await prisma.teamMember.updateMany({where:{invitedPhone:phone,userId:null},data:{userId:u.id}});res.json({token:sign(u),user:await getUser(u.id)})});
app.get("/me",auth,async(req,res)=>res.json(await getUser((req as any).user.id)));
app.put("/me/profile",auth,async(req,res)=>{const b=z.object({name:z.string().min(2).max(80)}).parse(req.body),u=await prisma.user.update({where:{id:(req as any).user.id},data:{name:b.name.trim()}});res.json({user:await getUser(u.id),token:sign(u)})});
app.post("/me/role",auth,async(req,res)=>{const id=(req as any).user.id,b=z.object({role:z.enum(["PLAYER","CAPTAIN"])}).parse(req.body);const data=b.role==="PLAYER"?{isPlayer:true,role:"PLAYER" as const}:{isCaptain:true,role:"CAPTAIN" as const};const u=await prisma.user.update({where:{id},data});res.json({user:await getUser(u.id),token:sign(u)})});
app.post("/me/switch-role",auth,async(req,res)=>{const id=(req as any).user.id,u=await prisma.user.findUnique({where:{id}});if(!u)return res.status(404).json({error:"User not found."});if(u.role==="CAPTAIN"){if(!u.isPlayer)await prisma.user.update({where:{id},data:{isPlayer:true}});const nu=await prisma.user.update({where:{id},data:{role:"PLAYER"}});return res.json({user:await getUser(id),token:sign(nu),activeRole:"PLAYER"})}if(u.role==="PLAYER"){if(!u.isCaptain)return res.status(400).json({error:"Captain profile is not set up yet."});const nu=await prisma.user.update({where:{id},data:{role:"CAPTAIN"}});return res.json({user:await getUser(id),token:sign(nu),activeRole:"CAPTAIN"})}return res.status(400).json({error:"Choose a role first."})});
app.get("/captain/team",auth,async(req,res)=>res.json(await prisma.team.findUnique({where:{captainId:(req as any).user.id},include:{members:{orderBy:{createdAt:"asc"},include:{user:{select:{id:true,name:true,phone:true,role:true}}}}}}));
app.post("/captain/team",auth,async(req,res)=>{const id=(req as any).user.id,b=z.object({name:z.string().min(2).max(80)}).parse(req.body),u=await prisma.user.findUnique({where:{id}});if(!u||!u.isCaptain)return res.status(403).json({error:"Captain access required."});if(await prisma.team.findUnique({where:{captainId:id}}))return res.status(409).json({error:"You already have a team."});const t=await prisma.team.create({data:{name:b.name.trim(),captainId:id}});await prisma.user.update({where:{id},data:{teamName:t.name,role:"CAPTAIN",isCaptain:true}});res.status(201).json(t)});
app.post("/captain/team/players",auth,async(req,res)=>{const id=(req as any).user.id,phone=z.string().min(8).max(30).parse(req.body.phone).trim(),team=await prisma.team.findUnique({where:{captainId:id}});if(!team)return res.status(400).json({error:"Create your team first."});if(await prisma.teamMember.findUnique({where:{teamId_invitedPhone:{teamId:team.id,invitedPhone:phone}}}))return res.status(409).json({error:"This number is already in your team."});let p=await prisma.user.findUnique({where:{phone}}),created=false;if(!p){p=await prisma.user.create({data:{phone,name:"New Player",role:"PLAYER",isPlayer:true}});created=true}else if(!p.isPlayer)p=await prisma.user.update({where:{id:p.id},data:{isPlayer:true}});const m=await prisma.teamMember.create({data:{teamId:team.id,userId:p.id,invitedPhone:phone}});res.status(201).json({createdProfile:created,devOtp:created?"123456":null,member:m})});
app.put("/captain/qr",auth,async(req,res)=>{const b=z.object({qrData:z.string().min(3).max(2000)}).parse(req.body),u=await prisma.user.update({where:{id:(req as any).user.id},data:{captainQrData:b.qrData,isCaptain:true}});res.json({user:await getUser(u.id)})});
app.get("/captain/matches",auth,async(req,res)=>res.json({matches:await prisma.match.findMany({where:{captainId:(req as any).user.id},include:{bookings:{include:{player:{select:{id:true,name:true,phone:true}}}}},orderBy:{startsAt:"asc"}})}));
app.post("/captain/matches",auth,async(req,res)=>{const id=(req as any).user.id,b=z.object({title:z.string().min(2),ground:z.string().min(2).optional(),startsAt:z.coerce.date(),endsAt:z.coerce.date()}).parse(req.body);if(b.endsAt.getTime()-b.startsAt.getTime()<10800000)return res.status(400).json({error:"Cricket matches must be at least 3 hours."});if(await prisma.match.findFirst({where:{captainId:id,startsAt:{lt:b.endsAt},endsAt:{gt:b.startsAt}}}))return res.status(409).json({error:"You already have a match at this time."});res.status(201).json(await prisma.match.create({data:{title:b.title,ground:b.ground??null,startsAt:b.startsAt,endsAt:b.endsAt,captainId:id}}))});
app.post("/captain/matches/:matchId/players",auth,async(req,res)=>{const id=(req as any).user.id,b=z.object({phone:z.string().min(8).optional(),playerId:z.string().optional()}).parse(req.body),match=await prisma.match.findFirst({where:{id:req.params.matchId,captainId:id}});if(!match)return res.status(404).json({error:"Match not found."});let p=b.playerId?await prisma.user.findUnique({where:{id:b.playerId}}):b.phone?await prisma.user.findUnique({where:{phone:b.phone.trim()}}):null,created=false;if(!p&&b.phone){p=await prisma.user.create({data:{phone:b.phone.trim(),name:"New Player",role:"PLAYER",isPlayer:true}});created=true}if(!p)return res.status(404).json({error:"Player not found."});if(await prisma.matchBooking.findUnique({where:{matchId_playerId:{matchId:match.id,playerId:p.id}}}))return res.status(409).json({error:"Player is already added to this match."});const booking=await prisma.matchBooking.create({data:{matchId:match.id,playerId:p.id,status:BookingStatus.ACCEPTED}});res.status(201).json({createdProfile:created,devOtp:created?"123456":null,booking})});
app.get("/captain/matches/:matchId/fees",auth,async(req,res)=>{const m=await prisma.match.findFirst({where:{id:req.params.matchId,captainId:(req as any).user.id},include:{bookings:{where:{status:BookingStatus.ACCEPTED},include:{player:{select:{id:true,name:true,phone:true}}}}}});if(!m)return res.status(404).json({error:"Match not found."});res.json({match:{id:m.id,title:m.title,ground:m.ground,startsAt:m.startsAt,totalFee:m.totalFee,feeSettled:m.feeSettled},players:m.bookings})});
app.post("/captain/matches/:matchId/settle-fees",auth,async(req,res)=>{const captainId=(req as any).user.id,b=z.object({totalFee:z.coerce.number().positive(),paidPlayerIds:z.array(z.string()).default([])}).parse(req.body),m=await prisma.match.findFirst({where:{id:req.params.matchId,captainId},include:{bookings:{where:{status:BookingStatus.ACCEPTED}}}});if(!m)return res.status(404).json({error:"Match not found."});if(!m.bookings.length)return res.status(400).json({error:"Add players to the match first."});const ids=new Set(b.paidPlayerIds),invalid=b.paidPlayerIds.filter(x=>!m.bookings.some(y=>y.playerId===x));if(invalid.length)return res.status(400).json({error:"One or more paid players are not in this match."});const share=Math.round((b.totalFee/m.bookings.length)*100)/100;await prisma.$transaction([prisma.match.update({where:{id:m.id},data:{totalFee:b.totalFee,feeSettled:true}}),...m.bookings.map(x=>prisma.matchBooking.update({where:{id:x.id},data:{feeAmount:share,feePaid:ids.has(x.playerId),paymentMarkedByPlayer:false,paymentMarkedAt:null}}))]);const paid=m.bookings.filter(x=>ids.has(x.playerId)).length;res.json({totalFee:b.totalFee,playerShare:share,paidPlayers:paid,pendingPlayers:m.bookings.length-paid,collected:Math.round(share*paid*100)/100,outstanding:Math.round(b.totalFee-share*paid*100)/100})});
app.get("/captain/fees",auth,async(req,res)=>{const rows=await prisma.matchBooking.findMany({where:{match:{captainId:(req as any).user.id},feeAmount:{gt:0},feePaid:false},include:{player:{select:{id:true,name:true,phone:true}},match:{select:{id:true,title:true,startsAt:true}}},orderBy:{match:{startsAt:"asc"}}});res.json({fees:rows,total:rows.reduce((s,x)=>s+x.feeAmount,0)})});
app.post("/captain/fees/:id/confirm",auth,async(req,res)=>{const b=await prisma.matchBooking.findFirst({where:{id:req.params.id,match:{captainId:(req as any).user.id},paymentMarkedByPlayer:true,feePaid:false}});if(!b)return res.status(404).json({error:"Payment confirmation not found."});res.json(await prisma.matchBooking.update({where:{id:b.id},data:{feePaid:true}}))});
app.get("/player/teams",auth,async(req,res)=>{const id=(req as any).user.id,m=await prisma.teamMember.findMany({where:{userId:id},include:{team:{include:{captain:{select:{id:true,name:true}}}}},orderBy:{createdAt:"desc"}}),b=await prisma.matchBooking.findMany({where:{playerId:id},include:{match:{include:{captain:{select:{id:true,name:true,teamName:true}}}}},orderBy:{match:{startsAt:"desc"}}});const out=new Map<string,any>();m.forEach(x=>out.set(x.team.id,{id:x.team.id,name:x.team.name,captain:x.team.captain.name,source:"Team",lastMatch:null}));b.forEach(x=>{const c=x.match.captain,k=c.id,e=out.get(k);if(e){if(!e.lastMatch||new Date(x.match.startsAt)>new Date(e.lastMatch))e.lastMatch=x.match.startsAt}else out.set(k,{id:k,name:c.teamName||"Team",captain:c.name,source:"Booked",lastMatch:x.match.startsAt})});res.json({teams:[...out.values()]})});
app.get("/player/matches",auth,async(req,res)=>res.json({matches:await prisma.matchBooking.findMany({where:{playerId:(req as any).user.id,status:{in:[BookingStatus.ACCEPTED,BookingStatus.PENDING]}},include:{match:{include:{captain:{select:{id:true,name:true,teamName:true,captainQrData:true}}}}},orderBy:{match:{startsAt:"asc"}}})}));
app.get("/player/pending-fees",auth,async(req,res)=>{const fees=await prisma.matchBooking.findMany({where:{playerId:(req as any).user.id,status:BookingStatus.ACCEPTED,feePaid:false,feeAmount:{gt:0}},include:{match:{include:{captain:{select:{name:true,teamName:true,captainQrData:true}}}}},orderBy:{match:{startsAt:"desc"}}});res.json({fees,total:fees.reduce((s,x)=>s+x.feeAmount,0)})});
app.post("/player/fees/:id/mark-paid",auth,async(req,res)=>{const b=await prisma.matchBooking.findFirst({where:{id:req.params.id,playerId:(req as any).user.id,status:BookingStatus.ACCEPTED,feePaid:false}});if(!b)return res.status(404).json({error:"Pending fee not found."});res.json(await prisma.matchBooking.update({where:{id:b.id},data:{paymentMarkedByPlayer:true,paymentMarkedAt:new Date()}}))});
app.get("/schedule",auth,async(req,res)=>{const id=(req as any).user.id;res.json({locks:await prisma.scheduleLock.findMany({where:{playerId:id},orderBy:{startsAt:"asc"}}),requests:await prisma.matchBooking.findMany({where:{playerId:id,status:BookingStatus.PENDING},include:{match:{include:{captain:{select:{name:true,teamName:true}}}}},orderBy:{match:{startsAt:"asc"}}})})});
app.post("/schedule/lock",auth,async(req,res)=>{const id=(req as any).user.id,b=z.object({title:z.string().min(2),startsAt:z.coerce.date(),endsAt:z.coerce.date()}).parse(req.body);if(b.endsAt<=b.startsAt)return res.status(400).json({error:"End time must be after start time."});if((await prisma.scheduleLock.findMany({where:{playerId:id,startsAt:{lt:b.endsAt},endsAt:{gt:b.startsAt},status:"LOCKED"}})).length)return res.status(409).json({error:"This time is already locked in your schedule."});res.status(201).json(await prisma.scheduleLock.create({data:{playerId:id,title:b.title,startsAt:b.startsAt,endsAt:b.endsAt,source:"PLAYER"}}))});
app.post("/bookings/:id/respond",auth,async(req,res)=>{const b=z.object({accept:z.boolean()}).parse(req.body),x=await prisma.matchBooking.findUnique({where:{id:req.params.id},include:{match:true}}),id=(req as any).user.id;if(!x||x.playerId!==id)return res.status(404).json({error:"Request not found."});res.json(await prisma.matchBooking.update({where:{id:x.id},data:{status:b.accept?BookingStatus.ACCEPTED:BookingStatus.DECLINED}}))});
app.listen(PORT,()=>console.log(`🏏 Cricket Manager API: http://localhost:${PORT}`));